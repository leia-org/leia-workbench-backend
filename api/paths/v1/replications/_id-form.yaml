patch:
  tags:
    - Replications v1
  summary: Change the form of a Replication
  operationId: updateReplicationForm
  parameters:
    - name: id
      in: path
      required: true
      description: Unique identifier for the replication
      schema:
        $ref: '../../../components/schemas/mongoId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            form:
              type: string
              description: New form for the replication
  responses:
    '200':
      description: Successful response with Replication
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/v1/replication/replication.yaml'
delete:
  tags:
    - Replications v1
  summary: Delete the form of a Replication
  operationId: deleteReplicationForm
  parameters:
    - name: id
      in: path
      required: true
      description: Unique identifier for the replication
      schema:
        $ref: '../../../components/schemas/mongoId.yaml'
  responses:
    '200':
      description: Successful response with Replication
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/v1/replication/replication.yaml'
