post:
  tags:
    - Interactions v1
  summary: Start a new Session for a Replication
  operationId: startSession
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../../components/schemas/v1/interaction/startSessionRequest.yaml'
  responses:
    '201':
      description: Replication created successfully
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/v1/interaction/sessionIdResponse.yaml'
