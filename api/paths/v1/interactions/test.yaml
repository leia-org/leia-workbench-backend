post:
  tags:
    - Interactions v1
  summary: Start a new test Session for a Replication
  operationId: startTestSession
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../../components/schemas/v1/interaction/startTestSessionRequest.yaml'
  responses:
    '201':
      description: Replication created successfully
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/v1/interaction/sessionIdResponse.yaml'
