type: object
properties:
  reflectiveAvailable:
    type: boolean
    description: A reflective interview can be started from this finished session's submitted solution.
  session:
    $ref: './session.yaml'
  messages:
    type: array
    items:
      $ref: './message.yaml'
  leia:
    $ref: '../manager/leia.yaml'
  replication:
    $ref: '../replication/replication.yaml'
  multiLeia:
    $ref: './multiLeia.yaml'
